// basic function and type conversion in typeScript
// type Salutation = {message:string,name:string};

// function greet({message,name}:Salutation):string {
//     return `${message}, ${name}`;
// }

// const message:string = greet({'name':'zn','message':'hello'});

// console.log(message);


// working with html element in TypeScript
// const input = document.getElementById("username") as HTMLInputElement;

// input.autofocus = true;

// input.addEventListener('keyup',(event) => {
//     // as currentTarget is a dom Element
//     const input = event.currentTarget as HTMLInputElement;
//     console.log(input.value);
// });


// generic on TypeScript




